name: Release disabled in private fork

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Release workflow intentionally disabled
        run: |
          echo "Runtime self-update/release publishing is disabled in Mike's private security fork."
          echo "Build reviewed binaries from source after CI passes."
          exit 1
